# =============================================================================
# AI CAREER PLATFORM WEB - LOCAL DEVELOPMENT ENVIRONMENT
# =============================================================================
# This is a template for local development. Copy this to .env.local:
#   cp .env.local .env.local (or just use this file directly)
# =============================================================================

# --- API Configuration -------------------------------------------------------
# Points to your local API server (must match the API's PORT in apps/api/.env)
NEXT_PUBLIC_API_URL=http://localhost:4000/api

# --- Site Configuration ------------------------------------------------------
# Public origin of this web app (no trailing slash)
# Used for canonical URLs, Open Graph tags, sitemap.xml, and robots.txt
NEXT_PUBLIC_SITE_URL=http://localhost:3000
